import { PropertiesClient } from '@/features/platform/components/properties-client';

export default function PropertiesPage() {
    return (
        <div className="p-4 md:p-6 lg:p-8">
            <PropertiesClient />
        </div>
    );
}
