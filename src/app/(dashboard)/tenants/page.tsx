import { TenantsClient } from '@/features/platform/components/tenants-client';

export default function TenantsPage() {
    return (
        <div className="p-4 md:p-6 lg:p-8">
            <TenantsClient />
        </div>
    );
}
