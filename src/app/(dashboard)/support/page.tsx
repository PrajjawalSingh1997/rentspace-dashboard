import { SupportClient } from '@/features/platform/components/support-client';

export default function SupportPage() {
    return (
        <div className="p-4 md:p-6 lg:p-8">
            <SupportClient />
        </div>
    );
}
