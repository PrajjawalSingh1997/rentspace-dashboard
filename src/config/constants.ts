export const ROUTES = {
    AUTH: {
        LOGIN: "/login",
    },
    DASHBOARD: {
        OVERVIEW: "/overview",
    },
} as const;

export const STORAGE_KEYS = {
    AUTH_STORE: "rentspace-admin-auth",
    THEME: "rentspace-theme",
} as const;
